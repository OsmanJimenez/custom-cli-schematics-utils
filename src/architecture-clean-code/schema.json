{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "architectureCleanCodeSchema",
  "title": "Architecture Clean Code Schema",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "The path where the architecture files will be generated.",
      "default": "src/app",
      "x-prompt": "What is the path where the files will be generated?"
    },
    "name": {
      "type": "string",
      "description": "Name of the entity",
      "minLength": 1,
      "x-prompt": "What is the name of the entity?"
    }
  },
  "required": ["name"]
}
